# 报告写作指南（Overleaf 协作）

> 五个人在 Overleaf 上写报告的分工、段落、交付物和写作主线。

---

## 报告结构（10 个部分）

1. Title and Group Members
2. Abstract
3. Introduction and Motivation
4. Dataset Description and Preprocessing
5. Methodology
6. Experimental Setup
7. Results and Analysis
8. Model Interpretation and Error Analysis
9. Conclusion
10. References and AI Tool Usage Statement

---

## 五人分工

### A 同学：总编 + Abstract + Introduction + Conclusion

| 负责段落 | 2. Abstract, 3. Introduction, 9. Conclusion, 10. AI Usage Statement, 最终统稿 |
|---------|------|
| 交付物 | 摘要、引言、结论、AI 使用声明、统一格式与编号 |

**Abstract（~150-250 words）**
- 任务：HAM10000/ISIC 2018 皮肤病灶 7 分类
- 方法：Baseline CNN、ResNet18、ConvNeXt-Tiny 迁移学习
- 数据策略：lesion-level split，避免 train/val 病灶泄漏
- 主要结果：ConvNeXt-Tiny 最优，Val Macro-F1 78.10%，Test Macro-F1 75.99%
- 结论：迁移学习明显优于 baseline，MEL/NV/BKL 仍然容易混淆

**Introduction（2-3 段）**
- 第一段：皮肤病灶分类任务背景
- 第二段：为什么深度学习适合图像分类
- 第三段：本项目目标和贡献（完整 pipeline，compare baseline vs 两个迁移模型）

**Conclusion（1-2 段）**
- 项目完成完整 pipeline
- ConvNeXt-Tiny 最优
- ResNet18 明显优于 baseline
- 类别不平衡和视觉相似类别仍是主要挑战
- 未来方向：focal loss、balanced sampler、stronger augmentation、ensemble、calibration

**最终统稿职责**
- 统一语气和格式
- Figure/Table 编号统一（Figure 1, Table 1, ...）
- 确保所有数值来自 `outputs/` 下的产物文件，不凭空杜撰
- 统一术语：Baseline CNN, ResNet18, ConvNeXt-Tiny, Macro-F1, Accuracy, Weighted-F1

---

### B 同学：Dataset + Preprocessing + Split

| 负责段落 | 4. Dataset Description and Preprocessing |
|---------|------|
| 所需文件 | `outputs/README.md` 的数据章节, `data/splits/`, `scripts/datadownload/` |
| 交付物 | 数据来源说明、预处理表、lesion-level split 段落、数据流程图 |

**4.1 Dataset source**
- HAM10000 作为训练/验证数据（7 类：MEL, NV, BCC, AKIEC, BKL, DF, VASC）
- 课程提供 TestSet 作为最终测试集（1511 张，缺 1 张 ISIC_0035068）
- TestSet 只用于最终报告，不参与模型选择

**4.2 Data preprocessing**
- 图像 resize 到 224×224
- 训练集：随机翻转、旋转、颜色抖动
- Val/Test：确定性 resize + normalize
- ImageNet mean/std normalization

**预处理表：**

| Component | Setting |
|-----------|---------|
| Image size | 224×224 |
| Train augmentation | Horizontal/vertical flip, rotation, color jitter |
| Normalization | ImageNet mean/std |
| Split strategy | Lesion-level stratified train/val split |
| Test usage | Final report only |

**4.3 Lesion-level split（关键亮点）**
- 不能简单 image-level random split
- HAM10000 中同一 `lesion_id` 可能有多张图像
- 当同一病灶图像同时进入 train 和 val 时会造成 validation leakage
- 项目使用 **lesion-level group split**：先按 lesion_id 分组，对 lesion group 做 stratified split，再映射回图像级
- Train: 8017 张，Val: 1998 张（20%），lesion overlap = 0

---

### C 同学：Methodology + Model Architectures

| 负责段落 | 5. Methodology |
|---------|------|
| 所需文件 | `src/ham_in_dl/models/`, `configs/*.yaml` |
| 交付物 | 三个模型介绍、模型设置表、loss/optimizer/class weights 说明 |

**5.1 Baseline CNN**
- 简单 CNN baseline，从零训练
- 3 层 Conv2d + ReLU + MaxPool + FC（见 `src/ham_in_dl/models/baseline_cnn.py`）
- 作为迁移学习的对比基准

**5.2 ResNet18**
- ImageNet pretrained
- 替换最终 FC 层为 7 类输出
- 全模型 fine-tune（不冻结 backbone）

**5.3 ConvNeXt-Tiny**
- ImageNet pretrained
- 替换 classifier head 为 7 类输出
- 全模型 fine-tune
- 作为更强的现代 CNN backbone

**5.4 Loss and imbalance handling**
- CrossEntropyLoss + class weights
- Adam optimizer
- Early Stopping 基于 validation Macro-F1

**模型设置表：**

| Model | Pretrained | Trainable | Epochs | Batch Size | LR | Best Epoch |
|-------|-----------|-----------|--------|------------|-----|------------|
| Baseline CNN | No | All | 30 | 256 | 1e-3 | 22 |
| ResNet18 | ImageNet | All | 25 | 128 | 1e-4 | 13 |
| ConvNeXt-Tiny | ImageNet | All | 25 | 128 | 1e-4 | 20 |

---

### D 同学：Experimental Results + Quantitative Analysis

| 负责段落 | 6. Experimental Setup, 7. Results and Analysis |
|---------|------|
| 所需文件 | `outputs/history_*.csv`, `outputs/val_*_metrics.json`, `outputs/test_convnext_tiny_*.json`, `outputs/figures/` |
| 交付物 | 实验设置段落、三模型对比表、test 结果表、per-class F1 表、训练曲线图与文字分析 |

**6. Experimental Setup**
- 所有模型使用相同 train/val split
- Val 用于模型比较和 early stopping
- Test 只对 ConvNeXt-Tiny 运行，不参与调参
- 指标：Accuracy, Precision, Recall, Macro-F1, Weighted-F1, Confusion Matrix
- Macro-F1 为主要比较指标（类别不平衡）

**7.1 Val 对比表（关键表）**

| Model | Best Epoch | Val Accuracy | Val Macro-F1 |
|-------|:----------:|:------------:|:------------:|
| Baseline CNN | 22 | 54.15% | 37.77% |
| ResNet18 | 13 | 79.73% | 71.12% |
| ConvNeXt-Tiny | 20 | 85.49% | 78.10% |

**7.2 Test 结果（ConvNeXt-Tiny）**

| Metric | Value |
|--------|:-----:|
| Accuracy | 84.71% |
| Macro Precision | 77.48% |
| Macro Recall | 75.97% |
| Macro-F1 | 75.99% |
| Weighted-F1 | 84.76% |

**7.3 Per-class Test F1**

| Class | F1-score | Support |
|-------|:-------:|:-------:|
| MEL | 67.84% | 171 |
| NV | 92.23% | 908 |
| BCC | 75.00% | 93 |
| AKIEC | 63.64% | 43 |
| BKL | 77.78% | 217 |
| DF | 78.05% | 44 |
| VASC | 77.42% | 35 |

**7.4 训练曲线分析**
- 用 `outputs/figures/loss_curves.png` — 三模型 loss 曲线
- 用 `outputs/figures/accuracy_curves.png` — 三模型 accuracy 曲线
- 用 `outputs/figures/overfitting_gap.png` — train-val F1 gap
- 用 `outputs/figures/parameter_comparison.png` — 参数量对比

**D 同学要写的分析结论：**
- Baseline 欠拟合（train 和 val 都低）
- ResNet18 明显提升，early stopping 在 epoch 13 后触发
- ConvNeXt-Tiny 最高，但 train-val gap 较大（~19%），存在一定过拟合
- Test 性能略低于 Val 但仍在合理范围，泛化可接受

---

### E 同学：Error Analysis + Grad-CAM

| 负责段落 | 8. Model Interpretation and Error Analysis |
|---------|------|
| 所需文件 | `outputs/test_convnext_tiny_confusion_matrix.csv`, `outputs/error_analysis/`, `outputs/gradcam/` |
| 交付物 | 混淆矩阵分析、高置信度失败案例、Grad-CAM 图与解释 |

**8.1 Confusion Matrix Analysis**

用 ConvNeXt-Tiny test confusion matrix。主要混淆对：

| True | Predicted | Count |
|------|-----------|:----:|
| NV | MEL | 34 |
| MEL | NV | 34 |
| BKL | NV | 24 |
| NV | BKL | 18 |
| BKL | MEL | 14 |
| BKL | AKIEC | 12 |

写作：MEL ↔ NV, BKL → NV/MEL/AKIEC 是主导混淆方向，视觉相似的色素性病灶对最强模型仍然有挑战。

**8.2 高置信度失败案例**

从 `outputs/error_analysis/top_failed.csv` 取：

| image_id | True | Pred | Confidence |
|----------|------|------|:----------:|
| ISIC_0036034 | MEL | NV | 0.9999 |
| ISIC_0034914 | BKL | NV | 0.9999 |
| ISIC_0035842 | AKIEC | MEL | 0.9987 |

写作：部分误判置信度极高，说明模型可能存在 calibration 问题。

**8.3 高置信度正确案例**

从 `outputs/error_analysis/top_correct.csv` 取：最高置信度正确样本大多是 NV 和 BKL。NV 样本量最大，模型学习最充分。

**8.4 Grad-CAM**

从 `outputs/gradcam/` 选取图片。推荐直接使用 `gradcam_comparison.png`，其中已包含 3 个正确 + 3 个失败样本的左右并排对照图（绿色=正确，红色=错误，标注 true/pred/confidence）。

也可单独选取 `correct_*` 和 `failed_*` 单张图：

| 类型 | 建议数量 |
|------|:------:|
| ConvNeXt correct cases | 2 |
| ConvNeXt failed cases | 2 |

> **当前 `outputs/gradcam/` 中所有图为 ConvNeXt-Tiny 的结果。**

写作：
- 正确预测时热力图集中在病灶主体区域
- 错误预测时模型可能关注背景、边界、毛发
- Grad-CAM 提供粗略可解释性，不能证明医学因果关系

---

## 写作工作流

### Step 1：每人独立写 Markdown

```
report/sections/
├── 01_intro_abstract_conclusion.md    # A 同学
├── 02_dataset_preprocessing.md        # B 同学
├── 03_methodology.md                  # C 同学
├── 04_results_analysis.md             # D 同学
└── 05_interpretability_errors.md      # E 同学
```

不要五个人同时改一份文件，容易冲突。Overleaf 上可以先各自写再合。

### Step 2：统一写作规范

- 语言：英文
- 模型名统一：`Baseline CNN`, `ResNet18`, `ConvNeXt-Tiny`
- 指标格式：`Macro-F1 78.10%`, `Accuracy 85.49%`
- 百分比保留两位小数
- 所有数值从 `outputs/` 产物文件取，不凭空杜撰
- 图注格式：`**Figure X.** Caption.`
- 表头格式：`**Table X.** Caption.`

### Step 3：A 同学最终统稿

合并顺序：
1. Title / Abstract / Introduction（A）
2. Dataset & Preprocessing（B）
3. Methodology（C）
4. Experimental Setup + Results（D）
5. Error Analysis + Grad-CAM（E）
6. Conclusion + References + AI Statement（A）

---

## 核心写作主线

报告不要写散，整条主线应该是：

1. 我们建立了简单 CNN baseline
2. 使用 ImageNet 预训练模型 ResNet18 和 ConvNeXt-Tiny 做迁移学习
3. **使用 lesion-level split** 避免数据泄漏
4. 迁移学习明显优于 baseline，ConvNeXt-Tiny 最优
5. ConvNeXt-Tiny 在 test set 上达到 84.71% Accuracy 和 75.99% Macro-F1
6. 错误集中在 MEL↔NV↔BKL 等视觉相似类别
7. Grad-CAM 显示模型在部分样本中关注病灶区域，但存在高置信度误判

## 注意：覆盖问题已修复

`evaluate.py` 的输出文件现在带模型名前缀：

- `val_convnext_tiny_confusion_matrix.csv`（而不是 `val_confusion_matrix.csv` 被覆盖）
- `test_convnext_tiny_metrics.json`（而不是 `test_metrics.json`）

**写报告前需重跑一次 val 评估**，确保所有 model-specific 产物到位：

```bash
python scripts/evaluate.py --config configs/baseline_cnn.yaml --split val
python scripts/evaluate.py --config configs/resnet18.yaml --split val
python scripts/evaluate.py --config configs/convnext_tiny.yaml --split val
```

跑完后旧的 `val_confusion_matrix.csv` 等无前缀文件可以删掉。

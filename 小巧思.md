# 小巧思

一些值得记录的设计决策、数据处理技巧和工程思路。

---

## 1. 数据流水线的统一入口

把"下载→解压→整理→切分→校验"拆成独立脚本，但提供一个总入口 `scripts/setup_data.py`：

```bash
python scripts/setup_data.py --force
```

新组员进项目只需跑一条命令，不需要理解 4 个 README 和 5 个脚本的内部细节。细分脚本留给调试用。

---

## 2. 按病灶 ID 分组切分，消除数据泄漏

HAM10000 中一个病灶（lesion）可能有多张不同角度/光照的图片。如果按"单张图片"随机切分，同一个病灶的图片可能分别落入 train 和 val，模型等于在验证集上"见过"训练数据。

实现方式（`src/ham_in_dl/data/split.py`）：

- 先按 `lesion_id` 分组，每组取多数标签
- 对 **病灶分组** 做 stratified split（按组的最常见标签分层）
- 最后再根据分组结果筛选出原始图片

结果：**train 和 val 的 lesion_id 交集为零，从源头杜绝了泄漏。**

---

## 3. 类别不平衡：class weights + stratified split 双保险

数据分布极度不均衡（NV:DF ≈ 58:1），两处做了对抗：

1. **CrossEntropyLoss 的 class weights**（`src/ham_in_dl/data/imbalance.py`）  
   用 `总样本数 / (类别数 × 每类样本数)` 计算逆频权重——样本越少的类别权重越大，迫使模型关注少数类。

2. **病灶 stratified split**（`src/ham_in_dl/data/split.py`）  
   切分时按 label 分层抽样，保证 train 和 val 里 7 个类别的比例大致相同，不会出现 val 里全是 NV 的极端情况。

---

## 4. raw → extracted → processed 三层数据隔离

数据不混放，分为三个独立层级：

| 层级 | 路径 | 说明 |
|---|---|---|
| raw | `data/raw/_downloads/` | 原始压缩包 + kagglehub 缓存，永不修改 |
| extracted | `data/raw/_downloads/ham10000_*/` | 临时解压产物，可删除重建 |
| processed | `data/processed/` | 项目统一格式，含标准化 `metadata.csv`，训练/评估从这里读 |

原则：raw 只读不写，processed 可以 `scripts/setup_data.py --force` 随时重建，splits 可进 git 共享。

---

## 5. split 与 processed 分离

`data/splits/train.csv` 和 `data/splits/val.csv` 是"实验定义文件"（约 2MB），和 `data/processed/`（约 4GB 图片）分开存放。

好处：
- split CSV 可以提交到 git，全组用同一份划分
- processed 在 gitignore 里，不污染仓库
- 换一个 split 策略不需要重新准备 processed 数据

---

## 6. prediction CSV 自带完整元数据

评估脚本 `scripts/evaluate.py` 输出的 predictions 不止是 "pred_label / true_label"，还附带：

| 字段 | 用途 |
|---|---|
| `image_id` | 溯源到 ISIC 原始 ID |
| `dataset` | 标记来自 train / val / test |
| `image_path` | 直接用路径回读图片做 Grad-CAM |
| `confidence` | 知道模型"有多确信"这个预测 |
| `correct` | 布尔值，快速筛选成功/失败样本 |

这让后续的错误分析（`scripts/analyze_errors.py`）和 Grad-CAM（`scripts/run_gradcam.py`）无需额外反查路径。

---

## 7. 测试集缺失图片的 fallback 策略

老师给的 `HAM10000_TestSet.zip` 中缺了 1 张图（`ISIC_0035068`），groundtruth 有 1512 行但图片只有 1511 张。

处理方式（`scripts/dataprocess/prepare_processed_data.py`）：
- 默认：记录到 `missing_images.txt`，从 metadata.csv 中剔除缺失项，让流程正常跑通
- 严格模式（`--strict`）：缺一张就报错终止，方便复查数据

这比没发现缺图直接报 `FileNotFoundError`、或者隐藏异常静默跳过要好得多。

---

## 8. 幂等设计：默认 copy 而非 move

数据处理脚本 `prepare_processed_data.py` 默认 `--mode copy`（复制），不是 `--mode move`（移动）。

- copy 保证了"脚本可以重复跑"，raw/extracted 源数据不会被改变
- move 保留为高级选项（省 4GB 磁盘空间时使用）

同样，解压脚本 `extract_archives.py` 支持 `--source` 选择解压源、`--skip-missing` 跳过缺失文件、`--force` 强制覆盖——任何一个环节缺了部分数据都不会阻断整个流程。

---

## 9. 数据校验独立于数据准备

`scripts/dataprocess/validate_data.py` 不和 `prepare` 逻辑混在一起，而是独立检查：

- `metadata.csv` 是否存在、必需列是否齐全
- label 是否都在 7 个合法类别内
- `image_path` 指向的文件是否存在
- 类别分布统计

这使得"数据处理流程改了很多次"以后，始终能用一行命令确认数据完整性。

---

## 10. 模型工厂 + 一行注册新模型

添加 ConvNeXt-Tiny 只改了 3 个地方：
- `transfer.py`：新增 ~12 行 builder
- `model_factory.py`：新增 ~6 行注册
- `configs/convnext_tiny.yaml`：新建配置文件

所有训练/评估/Grad-CAM 脚本都是 `build_model(config["model"], ...)` 统一入口，加新模型的成本极低。

---

## 11. 早停 + 以 macro-F1 为最佳指标

训练循环不单纯盯着 accuracy（会被多数类主导）。早停和 checkpoint 保存都以 **val macro-F1** 为准：

- 每次 epoch 结束时计算 val macro-F1
- 连续 N 个 epoch 没有提升即停止
- 只保存指标最高的 checkpoint

这在不平衡数据集上比用 accuracy 做早停指标更合理。

---

## 12. Grade-CAM 跨模型自动适配

`run_gradcam.py` 根据 config 里 `model` 字段自动选择正确的 target layer：
- ResNet18 → `model.layer4[-1]`
- ConvNeXt-Tiny → `model.features[-1]`

不需要为每个模型写单独的 Grad-CAM 脚本，新增模型时在脚本里加一行映射即可。
